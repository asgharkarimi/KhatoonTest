import 'package:flutter/material.dart';

class AddDocumentsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('افزودن مدارک'),
      ),
      body: Center(
        child: Text('صفحه افزودن مدارک'),
      ),
    );
  }
}
